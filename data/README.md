# Data Directory

| Subdir | Purpose |
|--------|---------|
| `raw/` | Raw OHLCV from MT5/yfinance (optional; some pipelines write directly to `clean/`) |
| `clean/` | Featured, labeled datasets and loss trades (`loss_trades.jsonl`) |
